Title at top in concise Chinese: "projectRoot 与 dataRoot 路径模型".

Draw two vertical roots. Left root: `projectRoot` points to real source folders. Right root: `dataRoot` points to local Alembic storage. Under dataRoot show `.asd`, Recipes, Candidates, Skills, Wiki, indexes. Show Ghost mode as a dashed bridge that keeps generated data out of the user repository.

Keep filesystem labels short and readable.
