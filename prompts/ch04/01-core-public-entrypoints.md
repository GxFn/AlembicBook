Title at top in concise Chinese: "@alembic/core public entrypoints".

Draw `src/index.ts` as the central public spine. Around it place entrypoint cards: workspace, knowledge, search, guard, project-context, recipe-context-capabilities, host-agent-workflows, daemon, plans. External consumers connect only to package exports, never to internal files.

Add a bottom principle strip: "public API first", "deterministic kernel", "shared contracts".
