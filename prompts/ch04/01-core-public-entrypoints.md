Title at top in concise Chinese: "@alembic/core public entrypoints".

Draw `src/index.ts` as the central public spine. Around it place entrypoint cards: workspace, knowledge, search, guard, project-intelligence, host-agent-workflows, decision-register. External consumers connect only to the spine, never to internal files.

Add a bottom principle strip: "public API first", "deterministic kernel", "shared contracts".
