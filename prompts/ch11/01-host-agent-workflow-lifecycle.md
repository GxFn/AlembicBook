Title at top in concise Chinese: "Host Agent 两个工作闭环".

Draw two separate horizontal loops.

Top purple loop "日常开发": "status" → "prime" → "work start" → "宿主执行" → "work finish" → "code_guard｜显式范围" → back to prime for the next task. Add small refs under the steps: primeRef, workRef, finishRef, guard result.

Bottom green loop "知识建设": "plan confirm" → "bootstrap / rescan" → "Mission Briefing" → "宿主读代码" → "submit knowledge" → "dimension complete". Show Candidate/Recipe review as an external human gate after submission.

Add one boundary card: "Mission Briefing ≠ 完成证据" and one route distinction: "宿主 Agent 路径" versus dashed "resident daemon job｜另一执行者".

Do not end in `decision`, Decision Register, or automatic Recipe promotion. Do not merge resident jobs with the host workflow. Keep tool names short and accurate.
