Title at top in concise Chinese: "host-agent workflow 生命周期".

Draw a lifecycle loop: status → recipe_map/graph orientation → bootstrap/rescan Mission Briefing → host agent reads code → submit candidate → dimension complete → prime/work/code_guard. Show daemon-backed jobs as a separate explicit route owned by main Alembic.

Emphasize that Mission Briefing is task guidance, not completed work.
