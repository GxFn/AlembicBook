Title at top in concise Chinese: "analysis → search → guard 内核关系".

Create a kernel relationship diagram. Left: source files and AST facts. Center: ProjectContext refs, RecipeContext indexes, source refs. Right: search results and Guard checks. Bottom: evolution decisions and reviewer signals, not an active public decision-record tool.

Show that Guard depends on grounded facts, not free-form opinion.
