Title at top in concise Chinese: "结构事实、知识投影与约束".

Use four parallel capability cards, not a single pipeline:
1. "ProjectContext｜结构事实" with short chips "9 类请求" and "11 语言解析".
2. "RecipeContext｜只读知识" with "detail / list / prime" and a small lock icon labeled "无写入".
3. "Search｜多信号检索" with "BM25 / vector" and "drifted 降权".
4. "Guard｜范围检查" with "正则 / AST / 跨文件" and "uncertainty".

Below the four cards, draw two large formulas: "Graph = 结构" and "Recipe Map = 结构 + 知识". Add one small dashed note: "SourceGraph｜资产存在，生产未接通".

Use black arrows only where data is actually shared; ProjectContext and RecipeContext must remain distinct. Do not draw Search → Guard as a mandatory single-line dependency. Absolutely no Decision Register, DEC identifiers, free-form opinion box, or automatic knowledge mutation. Keep 12–14 primary labels maximum.
