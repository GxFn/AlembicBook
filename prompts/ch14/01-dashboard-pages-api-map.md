Title at top in concise Chinese: "Dashboard 页面与 API 映射".

Draw Dashboard UI tabs on the left: Recipes, SPM, Candidates, Knowledge, Guard, Project Pyramid, Skills, Jobs, Help. Draw API client and `/api/v1` route families in the center. Draw local runtime state and generated API types drift gate on the right. Connect each tab to the route family it consumes.

Keep it as a UI-to-API map, not a marketing page.
