Title at top in concise Chinese: "Dashboard 页面与 API 映射".

Draw Dashboard UI tabs on the left: Overview, Knowledge, Candidates, Jobs, Guard, Settings. Draw API client and daemon HTTP routes in the center. Draw local runtime state on the right. Connect each tab to the route family it consumes.

Keep it as a UI-to-API map, not a marketing page.
