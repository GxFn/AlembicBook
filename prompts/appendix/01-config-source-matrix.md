Title at top in concise Chinese: "配置来源矩阵".

Draw a compact matrix mapping config sources to consumers: package config, environment variables, local registry, project scope, `.agents/skills`, daemon provider config. Consumers: CLI, daemon, Plugin, Agent, Dashboard, Codex.

Keep text short and table-like but readable.
