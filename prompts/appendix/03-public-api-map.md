Title at top in concise Chinese: "Public API map".

Draw `@alembic/core` at the center with public entrypoints around it. Connect `alembic-ai`, `@alembic/agent`, AlembicPlugin, and Dashboard API client as consumers. Mark internal imports as blocked.

Use red crossed boundary for private internals.
