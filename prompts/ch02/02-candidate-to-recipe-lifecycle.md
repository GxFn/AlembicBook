Title at top in concise Chinese: "Candidate 到可消费知识".

Draw a clear review path: "提交 Candidate" → "schema / evidence" → "相似度 / 合并" → "人工审阅" → decision fork.

Accepted path: "pending" → "staging / active" → "Recipe" → four consumers "Search", "Prime", "Guard", "Dashboard". Rejected path: "退回修改 / deprecated". Show `SourceRef` as required evidence under Candidate and Recipe.

Add a bottom freshness feedback loop: "代码变化" → "SourceRef 漂移" → "降权标记" → "Rescan / Evolution" → back to review.

Do not use a generic five-state lifecycle. Do not show acceptance as an automatic merge. Do not show Codex itself as a lifecycle state or final authority.
