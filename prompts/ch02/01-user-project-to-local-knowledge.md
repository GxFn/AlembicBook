Title at top in concise Chinese: "从用户项目到本地知识层".

Draw a journey from "源码仓库" to "ProjectScope" to "dataRoot" to "Candidate" to "Recipe". Show `projectRoot` staying inside the user's repository and `dataRoot` staying in Alembic's local storage. Add a small Codex Plugin branch that reads the local knowledge layer.

Emphasize privacy boundary and no remote service dependency.
