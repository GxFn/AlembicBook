Title at top in concise Chinese: "Codex Plugin tool surface 分层".

Draw four stacked layers: Codex / Claude Code MCP host, local runtime control tools (`status/init/job/runtime`), three agent-facing public workflow tools (`prime/work/code_guard`), and ProjectContext / RecipeContext tools (`recipe_map/search/graph/plan/submit_knowledge`). Add side labels for admin tools and daemon-less Plugin status (PDR-3). Show structuredContent returning facts to the host agent.

Keep tool names short; use exact identifier only for `alembic_*`.
