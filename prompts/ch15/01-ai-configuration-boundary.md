Title at top in concise Chinese: "AI 配置边界".

Draw three zones: provider-backed daemon jobs, Codex host-agent workflow, and local deterministic Core tools. Show provider keys only inside local configuration for daemon jobs. Show Codex path not requiring stored provider keys.

Add bottom rule: "配置边界 ≠ 知识边界".
