Title at top in concise Chinese: "主 Alembic runtime 启动链路".

Draw a startup chain from `alembic-ai` CLI to SetupService, ProjectRuntimeControl, daemon, HTTP API, Dashboard server, JobStore, and Core/Agent services. Use numbered dots for startup order.

Show local resident service as the center of the page.
