Title at top in concise Chinese: "Alembic 四个闭环".

Draw four balanced rounded loops around one central card labeled "可审阅知识". Use exactly these loop titles and short steps:
1. "身份"："host project" → "projectRoot" → "dataRoot" → "runtime 对齐".
2. "生产"："ProjectContext" → "Plan" → "Candidate" → "Recipe".
3. "消费"："RecipeContext" → "Search / Prime" → "Guard / Skill" → "Dashboard".
4. "新鲜度"："SourceRef" → "漂移检查" → "降权标记" → "Rescan / Evolution".

Use blue for identity, green for production, purple for consumption, and amber for freshness. Arrows must visibly return to the start of each loop. Keep projectRoot outside dataRoot; do not draw Guard as a Candidate-to-Recipe promotion step.

Bottom legend: "身份可信", "知识可审阅", "消费可诊断", "漂移可修复". No extra paragraphs, endpoint lists, or invented tools.
